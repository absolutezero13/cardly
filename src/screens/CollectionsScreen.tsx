import Screen from "@/components/Screen";

const CollectionsScreen = () => {
  return (
    <Screen
      title="Collections"
      subtitle="Your scanned cards will live here."
    />
  );
};

export default CollectionsScreen;